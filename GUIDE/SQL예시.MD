CREATE TABLE `KANBAN_CARD` (
	`TASK_NO`	NUMBER	NOT NULL,
	`BOARD_NO`	NUMBER	NOT NULL,
	`TASK_NAME`	VARCHAR2(50)	NOT NULL,
	`TASK_STATUS`	VARCHAR2(20)	NOT NULL	COMMENT '해야 할 일 / 진행중 / 완료 (TODO / DOING / DONE)',
	`TASK_CREATED_AT`	DATE	NOT NULL,
	`TASK_UPDATED_AT`	DATE	NULL,
	`PROJECT_MEMBER_NO`	NUMBER	NOT NULL,
	`Field`	VARCHAR(255)	NULL,
	`Field2`	VARCHAR(255)	NULL
);

CREATE TABLE `MEMO` (
	`MEMO_NO`	NUMBER	NOT NULL,
	`MEMBER_NO`	NUMBER	NOT NULL,
	`CALENDAR_EVENT_NO`	NUMBER	NOT NULL,
	`MEMO_NAME`	VARCHAR2(30)	NOT NULL,
	`MEMO_TEXT`	VARCHAR2(255)	NULL,
	`MEMO_CREATED_AT`	DATE	NOT NULL,
	`MEMO_UPDATED_AT`	DATE	NULL,
	`MEMO_TYPE`	VARCHAR2(12)	NULL	COMMENT '개인메모/캘린더메모'
);

CREATE TABLE `HEALTH_SURVEY_QUESTION` (
	`HEALTH_SURVEY_QUESTION_NO`	NUMBER	NOT NULL,
	`HEALTH_SURVEY_NO`	NUMBER	NOT NULL,
	`HEALTH_SURVEY_ORDER`	NUMBER	NULL,
	`HEALTH_SRUVEY_QUESTION_CONTENT`	VARCHAR2(1000)	NULL,
	`HEALTH_SURVEY_QUESTION_MIN_SCORE`	NUMBER	NULL,
	`HEALTH_SURVEY_QUESTION_MAX_SCORE`	NUMBER	NULL,
	`HEALTH_SURVEY_QUESTION_CREATED_AT`	DATE	NOT NULL,
	`HEALTH_SURVEY_QUESTION_UPDATED_AT`	DATE	NULL
);

CREATE TABLE `LEAVE_BALANCE` (
	`LEAVE_BALANCE_NO`	NUMBER	NOT NULL,
	`MEMBER_NO`	NUMBER	NOT NULL,
	`LEAVE_TYPE`	VARCHAR2(20)	NOT NULL	DEFAULT 'ANNUAL'	COMMENT '연차 / 대체휴무 / 특별휴가',
	`LEAVE_BALANCE_TOTAL_DAYS`	NUMBER	NOT NULL,
	`LEAVE_BALANCE_USED_DAYS`	NUMBER	NULL,
	`LEAVE_BALANCE_REMAIN_DAYS`	NUMBER	NULL,
	`LEAVE_BALANCE_YEAR`	CHAR(4)	NOT NULL,
	`LEAVE_BALANCE_UPDATED_AT`	DATE	NULL
);

CREATE TABLE `AGENCY` (
	`AGENCY_NO`	NUMBER	NOT NULL,
	`AGENCY_NAME`	VARCHAR2(30)	NULL,
	`AGENCY_CODE`	VARCHAR2(6)	NULL
);

CREATE TABLE `HEALTH_SURVEY_RESPONSE` (
	`HEALTH_SURVEY_RESPONSE_NO`	NUMBER	NOT NULL,
	`HEALTH_SURVEY_NO`	NUMBER	NOT NULL,
	`MEMBER_NO`	NUMBER	NOT NULL,
	`HEALTH_SURVEY_RESPONSE_TOTAL_SCORE`	NUMBER	NULL,
	`HEALTH_SURVEY_RESPONSE_STATUS`	CHAR(1)	NOT NULL	COMMENT '완료 / 미완료 (Y / N)',
	`HEALTH_SURVEY_RESPONSE_CREATED_AT`	DATE	NULL
);

CREATE TABLE `NOTIFICATION` (
	`NOTIFICATION_NO`	NUMBER	NOT NULL,
	`MEMBER_NO`	NUMBER	NOT NULL,
	`NOTIFICATION_NAME`	VARCHAR2(30)	NULL,
	`NOTIFICATION_TEXT`	VARCHAR2(260)	NULL,
	`NOTIFICATION_CREATED_AT`	DATE	NULL,
	`NOTIFICATION_STATUS`	CHAR(1)	NOT NULL	DEFAULT Y	COMMENT 'Y / N',
	`NOTIFICATION_TYPE`	VARCHAR2(10)	NULL
);

CREATE TABLE `MEMBER` (
	`MEMBER_NO`	NUMBER	NOT NULL,
	`AGENCY_NO`	NUMBER	NOT NULL,
	`MEMBER_NAME`	VARCHAR2(20)	NOT NULL,
	`MEMBER_ID`	VARCHAR2(20)	NOT NULL	COMMENT 'UNIQUE',
	`MEMBER_PASSWORD`	VARCHAR2(100)	NOT NULL,
	`MEMBER_EMAIL`	VARCHAR2(50)	NOT NULL,
	`MEMBER_PHONE`	VARCHAR2(15)	NOT NULL,
	`MEMBER_STATUS`	VARCHAR2(20)	NOT NULL	COMMENT '활성 / 휴면 (ACTIVE /  BLOCKED)',
	`MEMBER_PROFILE_IMAGE`	VARCHAR2(100)	NULL,
	`MEMBER_PROFILE_BANNER_IMAGE`	VARCHAR2(100)	NULL,
	`MEMBER_ROLE`	VARCHAR2(20)	NOT NULL	COMMENT '아티스트 / 에이전시 관리자 / 담당자',
	`MEMBER_CREATED_AT`	DATE	NOT NULL,
	`MEMBER_UPDATED_AT`	DATE	NULL
);

CREATE TABLE `PROJECT` (
	`PROJECT_NO`	NUMBER	NOT NULL,
	`PROJECT_NAME`	VARCHAR2(100)	NOT NULL,
	`PROJECT_STATUS`	VARCHAR2(100)	NOT NULL	DEFAULT 연재	COMMENT '연재 / 휴재 / 완결',
	`PROJECT_COLOR`	VARCHAR2(50)	NOT NULL	DEFAULT 기본색
);

CREATE TABLE `ATTENDANCE_REQUEST` (
	`ATTENDANCE_REQUEST_NO`	NUMBER	NOT NULL,
	`MEMBER_NO`	NUMBER	NOT NULL,
	`ATTENDANCE_REQUEST_TYPE`	VARCHAR2(20)	NOT NULL	COMMENT '연차 / 병가 / 워케이션 / 재택 / 휴재',
	`ATTENDANCE_REQUEST_START_DATE`	DATE	NOT NULL,
	`ATTENDANCE_REQUEST_END_DATE`	DATE	NOT NULL,
	`ATTENDANCE_REQUEST_USING_DAYS`	NUMBER	NOT NULL,
	`ATTENDANCE_REQUEST_REASON`	VARCHAR2(255)	NULL,
	`WORKCATION_LOCATION`	VARCHAR2(100)	NULL	COMMENT '워케이션 신청 시에만 작성',
	`MEDICAL_FILE_URL`	VARCHAR2(500)	NOT NULL,
	`ATTENDANCE_REQUEST_STATUS`	VARCHAR2(20)	NOT NULL	DEFAULT 'PENDING'	COMMENT '신청 / 승인 / 반려 / 취소',
	`ATTENDANCE_REQUEST_REJECT_REASON`	VARCHAR2(200)	NULL,
	`ATTENDANCE_REQUEST_CREATED_AT`	DATE	NOT NULL,
	`ATTENDANCE_REQUEST_UPADATED_AT`	DATE	NULL
);

CREATE TABLE `CALENDAR_EVENT` (
	`CALENDAR_EVENT_NO`	NUMBER	NOT NULL,
	`MEMBER_NO`	NUMBER	NOT NULL,
	`CALENDAR_EVENT_NAME`	VARCHAR2(50)	NULL,
	`CALENDAR_EVENT_CONTENT`	VARCHAR2(100)	NULL,
	`CALENDAR_EVENT_TYPE`	VARCHAR2(20)	NULL,
	`CALENDAR_EVENT_STARTED_AT`	DATE	NOT NULL,
	`CALENDAR_EVENT_ENDED_AT`	DATE	NOT NULL,
	`CALENDAR_EVENT_CREATED_AT`	DATE	NOT NULL,
	`CALENDAR_EVENT_UPDATED_AT`	DATE	NULL,
	`CALENDAR_EVENT_COLOR`	VARCHAR(10)	NULL
);

CREATE TABLE `HEALTH_SURVEY` (
	`HEALTH_SURVEY_NO`	NUMBER	NOT NULL,
	`HEALTH_SURVEY_NAME`	VARCHAR2(100)	NULL,
	`HEALTH_SURVEY_TYPE`	VARCHAR2(30)	NULL	COMMENT '데일리/월간신체/월간정신',
	`HEALTH_SURVEY_CONTENT`	VARCHAR2(30)	NULL,
	`HEALTH_SURVEY_STATUS`	CHAR(1)	NOT NULL	COMMENT '미검진 / 검진완료 ( Y / N )',
	`HEALTH_SURVEY_CREATED_AT`	DATE	NULL,
	`HEALTH_SURVEY_UPDATED_AT`	DATE	NULL
);

CREATE TABLE `KANBAN_BOARD` (
	`KANBAN_BOARD_NO`	NUMBER	NOT NULL,
	`PROJECT_NO`	NUMBER	NOT NULL,
	`KANBAN_BOARD_NAME`	VARCHAR2(100)	NULL,
	`KANBAN_BOARD_ORDER`	NUMBER	NOT NULL,
	`KANBAN_BOARD_STATUS`	CHAR(1)	NOT NULL	DEFAULT Y	COMMENT 'Y / N'
);

CREATE TABLE `HEALTH_SURVEY_RESPONSE_ITEM` (
	`HEALTH_SURVEY_RESPONSE_ITEM_NO`	NUMBER	NOT NULL,
	`HEALTH_SURVEY_RESPONSE_NO`	NUMBER	NOT NULL,
	`HEALTH_SURVEY_QUESTION_NO`	NUMBER	NOT NULL,
	`HEALTH_SURVEY_QUESTION_ITEM_ANSWER_SCORE`	NUMBER	NOT NULL	COMMENT '0 / 1 / 2 / 3',
	`HEALTH_SURVEY_QUESTION_ITEM_CREATED_AT`	DATE	NOT NULL
);

CREATE TABLE `ATTENDANCE` (
	`ATTENDANCE_NO`	NUMBER	NOT NULL,
	`MEMBER_NO`	NUMBER	NOT NULL,
	`AGENCY_NO`	NUMBER	NOT NULL,
	`ATTENDANCE_TYPE`	VARCHAR2(10)	NOT NULL	COMMENT ''출근', '퇴근'',
	`ATTENDANCE_TIME`	DATE	NOT NULL	DEFAULT SYSDATE
);

CREATE TABLE `COMMENT` (
	`COMMENT_NO`	NUMBER	NOT NULL,
	`TASK_NO`	NUMBER	NOT NULL,
	`COMMENT_CONTENT`	VARCHAR2(1000)	NULL
);

CREATE TABLE `TASK_HISTORY` (
	`TASK_HISTORY_NO`	NUMBER	NOT NULL,
	`TASK_NO`	NUMBER	NOT NULL,
	`BEFORE_TASK_HISTORY`	VARCHAR2(1000)	NULL,
	`AFTER_TASK_HISTORY`	VARCHAR2(1000)	NULL,
	`TASK_HISTORY_UPDATED_AT`	DATE	NULL,
	`MEMBER_NO`	NUMBER	NOT NULL
);

CREATE TABLE `PROJECT_MEMBER` (
	`PROJECT_MEMBER_NO`	NUMBER	NOT NULL,
	`MEMBER_NO`	NUMBER	NOT NULL,
	`PROJECT_NO`	NUMBER	NOT NULL,
	`PROJECT_MEMBER_ROLE`	VARCHAR2(50)	NOT NULL	DEFAULT MEMBER	COMMENT 'LEADER / MEMBER'
);

ALTER TABLE `KANBAN_CARD` ADD CONSTRAINT `PK_KANBAN_CARD` PRIMARY KEY (
	`TASK_NO`
);

ALTER TABLE `MEMO` ADD CONSTRAINT `PK_MEMO` PRIMARY KEY (
	`MEMO_NO`
);

ALTER TABLE `HEALTH_SURVEY_QUESTION` ADD CONSTRAINT `PK_HEALTH_SURVEY_QUESTION` PRIMARY KEY (
	`HEALTH_SURVEY_QUESTION_NO`
);

ALTER TABLE `LEAVE_BALANCE` ADD CONSTRAINT `PK_LEAVE_BALANCE` PRIMARY KEY (
	`LEAVE_BALANCE_NO`
);

ALTER TABLE `AGENCY` ADD CONSTRAINT `PK_AGENCY` PRIMARY KEY (
	`AGENCY_NO`
);

ALTER TABLE `HEALTH_SURVEY_RESPONSE` ADD CONSTRAINT `PK_HEALTH_SURVEY_RESPONSE` PRIMARY KEY (
	`HEALTH_SURVEY_RESPONSE_NO`
);

ALTER TABLE `NOTIFICATION` ADD CONSTRAINT `PK_NOTIFICATION` PRIMARY KEY (
	`NOTIFICATION_NO`
);

ALTER TABLE `MEMBER` ADD CONSTRAINT `PK_MEMBER` PRIMARY KEY (
	`MEMBER_NO`
);

ALTER TABLE `PROJECT` ADD CONSTRAINT `PK_PROJECT` PRIMARY KEY (
	`PROJECT_NO`
);

ALTER TABLE `ATTENDANCE_REQUEST` ADD CONSTRAINT `PK_ATTENDANCE_REQUEST` PRIMARY KEY (
	`ATTENDANCE_REQUEST_NO`
);

ALTER TABLE `CALENDAR_EVENT` ADD CONSTRAINT `PK_CALENDAR_EVENT` PRIMARY KEY (
	`CALENDAR_EVENT_NO`
);

ALTER TABLE `HEALTH_SURVEY` ADD CONSTRAINT `PK_HEALTH_SURVEY` PRIMARY KEY (
	`HEALTH_SURVEY_NO`
);

ALTER TABLE `KANBAN_BOARD` ADD CONSTRAINT `PK_KANBAN_BOARD` PRIMARY KEY (
	`KANBAN_BOARD_NO`
);

ALTER TABLE `HEALTH_SURVEY_RESPONSE_ITEM` ADD CONSTRAINT `PK_HEALTH_SURVEY_RESPONSE_ITEM` PRIMARY KEY (
	`HEALTH_SURVEY_RESPONSE_ITEM_NO`
);

ALTER TABLE `ATTENDANCE` ADD CONSTRAINT `PK_ATTENDANCE` PRIMARY KEY (
	`ATTENDANCE_NO`
);

ALTER TABLE `COMMENT` ADD CONSTRAINT `PK_COMMENT` PRIMARY KEY (
	`COMMENT_NO`
);

ALTER TABLE `TASK_HISTORY` ADD CONSTRAINT `PK_TASK_HISTORY` PRIMARY KEY (
	`TASK_HISTORY_NO`
);

ALTER TABLE `PROJECT_MEMBER` ADD CONSTRAINT `PK_PROJECT_MEMBER` PRIMARY KEY (
	`PROJECT_MEMBER_NO`
);

